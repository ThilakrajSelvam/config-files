spring:
  datasource:
    platform: mysql
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/playground-qa
    username: ${dbusername}
    password: ${dbpassword}
    initialization-mode: always
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      authentication: TOKEN
      token: s.zK9vQWBLbavEHlaqUV7dSJgL
      kv-version: 2    