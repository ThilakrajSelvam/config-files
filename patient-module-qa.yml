spring:
  datasource:
    platform: mysql
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/playground-qa
    username: ${dbusername}
    password: ${dbpassword}
    initialization-mode: always
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      authentication: TOKEN
      token: s.DIY7Rad1ABs0RuzY3kZNdyDt
      kv-version: 2    